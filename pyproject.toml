[project]
name = "timelake"
version = "0.1.0"
description = "A time series feature framework"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "deltalake>=0.25.5",
    "polars>=1.27.1",
]
